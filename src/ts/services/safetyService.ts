// src/ts/services/safetyService.ts

export class SafetyService {
  constructor() {
    // Init eller laddning av whitelist/blacklist etc.
  }

  isPoolSafe(pool: any): boolean {
    // TODO: implementera riktiga safety checks
    return true;
  }
}
